# Atlas A2A adapter

This directory will contain the minimal official-A2A server adapter that serves
Atlas's Agent Card and task lifecycle. It is protocol hosting, not robot control
or a second custom agent runtime.
