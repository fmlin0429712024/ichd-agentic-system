# Mira A2A adapter

This directory will contain the minimal official-A2A client adapter used to
discover Atlas, validate capability compatibility, dispatch tasks, and consume
status and artifacts. It is transport wiring, not a custom agent runtime.
